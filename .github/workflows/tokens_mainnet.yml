name: Fetch Tokens Mainnet
on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

env:
  IC_HOST: https://ic0.app
  IC_ENVIRON: mainnet
  IC_HTTP: https://4fe7g-7iaaa-aaaak-aegcq-cai.raw.ic0.app

jobs:
  call-workflow:
    uses: infinity-swap/token-lists/.github/workflows/fetch_tokens.yml
